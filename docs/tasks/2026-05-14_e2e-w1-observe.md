# E2E v0.1 관찰 모드 착수 — W1 PoC + 야간 관찰 루프

| 항목   | 값         |
| ------ | ---------- |
| 작성일 | 2026-05-14 |
| 상태   | 진행 중    |
| Commit | —          |

야간 자동 관찰 학습 v0.1 — MultiTool 트리·`.mtproject` 스냅샷·Gemma4 응답 누적 (변경 0, 읽기 전용).

## 완료조건

- `skills/e2e_explorer/` 7개 파일 작성 (SKILL·requirements·ollama_client·ui_driver·observer·kb_store·orchestrator)
- 단위 테스트 4건 통과 (Ollama health, ollama chat ping, ui_driver dump, observer snapshot)
- 통합 dry-run 1건 통과 (`orchestrator --until-minutes 2`)
- Task Scheduler `E2E_Nightly` (00:00) + `E2E_Nightly_Kill` (05:30) 등록 + `Get-ScheduledTask` 확인
- 본 task 파일 Diff 섹션에 commit hash + 검증 결과 기록

## 금지조건

- MultiTool GUI 입력(click/type/keystroke) 코드 일체 작성 금지 — v0.1은 읽기 전용
- `.mtproject`·`.exp` 등 MultiTool 산출 파일 변경 금지 — 스냅샷 복사만
- `MultiToolProject/E2EProject/` 외 경로 학습 대상 포함 금지
- `kb/patterns/`, `kb/workflows.jsonl` 작성 금지 — v0.1 범위 아님 (W3.5+)
- Claude routine 등록 금지 — 현재는 야간 단방향만 (Claude routine은 W3에 별도 등록)

## 검증조건

| 단계         | 검증 방법                                                                                                                |
| ------------ | ------------------------------------------------------------------------------------------------------------------------ |
| T1 health    | `py skills/e2e_explorer/ollama_client.py --ping` → `health: True`                                                        |
| T2 chat      | `py skills/e2e_explorer/ollama_client.py --message "Reply with: PONG"` → `content: PONG`                                 |
| T3 ui_driver | MultiTool 떠 있는 상태에서 `py skills/e2e_explorer/ui_driver.py --dump --out tree.json` → JSON 생성                      |
| T4 observer  | `py skills/e2e_explorer/observer.py --project <p> --out /tmp/snap --label test` → 스냅샷 파일 생성                       |
| T5 dry-run   | `py skills/e2e_explorer/orchestrator.py --until-minutes 2 --interval 30` → `logs/e2e/<date>/observations.jsonl` 라인 ≥ 2 |
| T6 task      | `Get-ScheduledTask -TaskName E2E_Nightly` 존재 + State Ready                                                             |

## Diff

| Test | 결과 | 비고                                                                                  |
| ---- | ---- | ------------------------------------------------------------------------------------- |
| T1   | ✓    | `health: True` (gemma4:26b)                                                           |
| T2   | ✓    | `content: PONG`                                                                       |
| T3   | ✓    | `logs/e2e/_test_tree.json` 8.7KB, MultiTool Creator 8.4 메인 창 트리                  |
| T4   | ✓    | `.mtproject` + 2개 `.exp` 스냅샷 (sha256 기록)                                        |
| T5   | ✓    | 2분 dry-run: steps=2, trees=2, snapshots=2, llm_calls=2, errors=0 (Gemma 한국어 응답) |
| T6   | ✓    | `E2E_Nightly` (Daily 00:00) + `E2E_Nightly_Kill` (Daily 05:30) State=Ready            |

야간 작업 시간: **00:00 ~ 05:30**.

오늘 밤 자정부터 자동 관찰 학습 시작 예정.

commit hash: `0a80abc` — feat(e2e): E2E 자율 학습 v0.1 — 야간 관찰 모드 + Task Scheduler
